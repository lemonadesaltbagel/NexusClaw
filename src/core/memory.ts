// ---------------------------------------------------------------------------
// Memory prompt section builder (stub)
// ---------------------------------------------------------------------------

/** Build the memory section for the system prompt. */
export function buildMemoryPromptSection(): string {
  // TODO: implement memory loading and prompt section building
  return "";
}
