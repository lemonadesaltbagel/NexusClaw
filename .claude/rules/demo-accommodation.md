When the user provides demo code to integrate:

1. Follow the user's business logic exactly — do not change the design or control flow.
2. Fix details — the demo may have minor errors (wrong imports, typos, missing types), so adapt it to fit the project correctly.
3. Point out obvious issues if something looks clearly wrong.
4. Do NOT implement referenced functions unless the implementation is very simple and has only one right answer. Leave a stub for unfinished functions.
